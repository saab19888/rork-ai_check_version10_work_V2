import React from 'react';
import { View } from 'react-native';

// This is a dummy component since the logout functionality is handled in the tab bar
// Users will never actually see this screen as the logout action is intercepted
export default function LogoutScreen() {
  return <View />;
}